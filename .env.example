# Local secrets — copy to .env (gitignored). Never commit real keys.

# FB/IG social publish (morning Today + evening week-ahead + Tuesday meditation)
ZERNIO_API_KEY=

# HeyGen reels (TikTok / YouTube Shorts) — optional until API generate is wanted
HEYGEN_API_KEY=
HEYGEN_AVATAR_ID=
HEYGEN_VOICE_ID=
# Optional: HeyGen asset id after uploading a store background plate
HEYGEN_BACKGROUND_ASSET_ID=
